// OAuth routes removed - using JWT authentication instead
// If you need OAuth in the future, restore from git history

import type { Express } from "express";

export function registerOAuthRoutes(app: Express) {
  // OAuth routes disabled
}
